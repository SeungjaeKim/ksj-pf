(function (root) {
    root.EduFlowState = {
        createStore: function () {
            return {
                currentBranchId: "gangnam",
                selectedLeadId: "lead-minseo",
                selectedStudentId: "student-jiyun"
            };
        }
    };
}(this));
